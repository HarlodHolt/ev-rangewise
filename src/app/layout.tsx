import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "EV RangeWise — Find your Australian EV",
  description:
    "Find the right electric vehicle for your Australian lifestyle. Real-world ranges, real Australian conditions. No sales spin.",
  icons: {
    icon: "/favicon.ico",
  },
  openGraph: {
    title: "EV RangeWise",
    description:
      "The EV shortlist made for you. Built for Australians, by Australians.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en-AU"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
